---
name: a-share-sector-analysis-akshare
description: A股申万二级行业(124个)板块行情分析，纯 Python + akshare 实现，不依赖任何 MCP 连接器。按周涨幅/月涨幅双维度排行，生成图表与上涨原因分析，输出单文件 HTML 报告，报告开头附近两日影响股市的重要新闻 Top10。只做涨幅榜，不出跌幅榜。触发场景：板块行情、行业涨幅、板块排名、哪些板块涨了、行业轮动、板块分析、sector performance、申万二级。
agent_created: true
---

# A股板块行情分析（akshare 独立版）

对申万二级行业（124 个）做多周期**涨幅**分析，输出双维度涨幅排行、可视化图表与上涨原因解读，最终产出一份**自包含单文件 HTML 报告**，报告开头附**近两日影响股市的重要新闻 Top 10**。

**与 `a-share-sector-analysis` 的区别**：那个依赖 `westock-mcp` 连接器（实时但需用户启用）；本 skill **零 MCP 依赖**，靠 akshare 直连申万宏源研究，随时可跑，代价是数据滞后约 2 个交易日。

**无需用户提供参数**，默认行为：申万二级 + 周涨幅排序 + 月涨幅排序 + 双维度 Top10 上涨原因分析 + 近两日要闻 Top 10。

**只看涨幅**：本 skill 不生成跌幅榜，不做"最弱方向""资金撤离方向"分析，图表不使用绿色。用户明确要求，2026-09-17。

**输出 HTML 报告**：报告落成一份自包含单文件 HTML，**排版以「易读」为第一目标**，具体要求见 Step 5。用户明确要求，2026-09-18。

**开篇要闻**：报告最前面必须放「近两日影响股市的重要新闻」，最多 10 条、每条不超过 100 字。用户明确要求，2026-09-18。

---

## 环境准备

只需一个带 akshare 的 Python 3 环境：

```bash
# 已装好时直接验证
python -c "import akshare; print(akshare.__version__)"

# 未安装时（一次性）
pip install akshare
```

后续所有命令用 `PY=python`（**取数用**，需要 akshare）。
`calc_changes.py` 是纯标准库，用任意 Python 3 均可。

> **不要用东方财富接口**（`stock_board_industry_name_em` 等）：在部分网络环境下会被代理拦截，抛 `ProxyError`。申万宏源接口可直连。

> **不要在 Bash 里拼 coreutils 管道**（`ls`/`base64`/`head` 可能全部 not found）。列目录用 Glob，读文件用 Read，写文件用 Write。

---

## 工作流程

### Step 1 · 抓取板块列表与 K 线

```bash
PY=python
"$PY" scripts/fetch_sw2.py --out-dir ./data --workers 6
```

产出：
- `data/sectors.json` — 131 个申万二级行业（含 `上级行业`，可直接映射一级行业）
- `data/kline_data.json` — 每个行业最近 70 根日 K

脚本逻辑：
1. `ak.sw_index_second_info()` 取行业清单（代码形如 `801016.SI`，含 `上级行业` 字段）
2. 6 线程并发调用 `ak.index_hist_sw(symbol=code, period='day')`，失败自动重试 3 次
3. **剔除停更的僵尸指数**——申万已停止发布部分老二级指数（如 801011 林业、801019 农业综合Ⅱ，最后一根 K 线停在 2024 年）。判据：最后一根 K 线日期 < 全体最新日期则丢弃。剔除后正好 **124 个**
4. 元数据里记录 `latestDate`、`droppedStale`、`errorCount`

耗时约 15 秒。

### Step 2 · 计算多周期涨幅并排序

```bash
CALC=python

# 周涨幅（Top 30）
"$CALC" scripts/calc_changes.py --input data/kline_data.json --sector-info data/sectors.json \
  --sort chg_5d  --format tsv --fields name,parent,chg_5d,chg_10d,chg_20d,chg_60d --limit 31

# 月涨幅（Top 30）
"$CALC" scripts/calc_changes.py --input data/kline_data.json --sector-info data/sectors.json \
  --sort chg_20d --format tsv --fields name,parent,chg_20d,chg_5d,chg_10d,chg_60d --limit 31
```

**只用 `--limit`，不要用 `--bottom`**（脚本保留了该参数，但本 skill 不再调用）。

输出字段：`code` `name` `parent` `close` `chg_5d` `chg_10d` `chg_20d` `chg_60d`
（`--limit N` 需 +1 因为第一行是表头。）

口径：`chg_Nd = (最新收盘 - N个交易日前收盘) / N个交易日前收盘`，用交易日而非自然日。
`chg_5d` ≈ 一周，`chg_20d` ≈ 一月，`chg_60d` ≈ 一季（两月）。

**补充统计（必须做）**：脚本自带的 `Up(5d)` 只统计 `--limit` 之后的行数，不能反映全市场广度。要点解读里的「上涨家数 / 占比 / 极差 / 中位数」和「一级行业均值」需要另算全样本，可写一个 20 行左右的小脚本遍历 `kline_data.json`：

- 近 5 日、近 20 日的上涨家数、占比、最大/最小涨幅、极差、中位数
- 按 `parent` 聚合的一级行业均值排行
- 两个窗口各自的起始交易日（用于报告里写明区间）

### Step 3 · 图表

在 HTML 报告里**内联渲染**周涨幅 Top 15 横向柱状图——用纯 CSS 柱状图（`.bar-track` + `.bar-fill`，宽度按涨幅占最大值的比例换算）或内联 SVG，**不要引用外部图表库或 CDN**。

- 榜单里全是上涨板块，**柱子统一用红色 `#ef4444`**（A 股红涨惯例），不使用绿色
- 深色主题下：刻度文字 `#E6EDF3`、网格 `rgba(255,255,255,0.07)`
- 柱尾标注数值，图例注明"涨幅（红）· 数据源：申万宏源研究"

只有在用户明确要求图片文件时，才跑 `scripts/render_chart.py` 输出 PNG 并用 `present_files` 单独呈现。

### Step 4 · 检索上涨原因

**周涨幅 Top 10**、**月涨幅 Top 10** 各用 `WebSearch` 检索。
查询模板：`"A股 {板块名} 上涨 原因 {当前年月}"`（涨幅极大的可加 "大涨"/"涨停潮"）。

覆盖四个维度：产业/政策催化、业绩驱动、资金面、商品价格。
注意识别**同源行情**（例如数字媒体/影视院线/出版同属 AI 长剧催化，应在报告里合并归因，不要重复写三遍）。

### Step 4.5 · 检索近两日要闻

在 Step 4 的板块检索之外，**另起一组检索**汇总近两日影响股市的新闻：

- 查询模板：`"A股 影响 消息 {当天日期}"`、`"A股 复盘 {当天与前一日日期}"`、`"隔夜 美股 黄金 原油 {当天日期}"`
- 范围限定 **报告生成当天 + 前一天**（约 48 小时）
- 交叉核对多个来源，避免把单一自媒体的转述当成事实；优先使用带具体数字（利率、金额、产量、价格、订单额）的消息

### Step 5 · 输出 HTML 分析报告

产出一份**自包含单文件 HTML**，文件名 `A股申万二级板块行情分析_<YYYY-MM-DD>.html`，落在工作区根目录。

**硬性要求**：样式全部内联在 `<style>` 里，图表用纯 CSS / 内联 SVG，**不引用任何外部 CDN、字体或图片资源**——离线打开预览也要能完整、正确地渲染。

生成后用 `present_files` 呈现（会自动打开预览面板）；对话里另给 **10 行以内**的摘要（几个关键数字 + 三条主线 + 风险要点），不要把 HTML 内容再复述一遍。

#### 开篇要闻板块（必做，放在报告最前面）

报告最前面（标题 + 数据说明之后、周涨幅排行之前）必须放一节 **「近两日影响股市的重要新闻」**：

- **范围**：报告生成当天 + 前一天，约 48 小时内的消息
- **条数**：最多 10 条，精选，不要凑数
- **每条不超过 100 个中文字**（含标点），写成一段，不分行、不加子项目
- 每条格式：`【类型】核心事实（带关键数字）——影响方向 / 受益或承压板块`
  - 类型标签用：宏观 / 政策 / 产业 / 商品 / 海外 / 资金 六类之一
- 只收**能传导到股市**的消息（货币政策、财政与产业政策、商品价格、海外市场与地缘、龙头企业业绩与重大合同、资金面），不写与市场无关的社会新闻、也不写无实质影响的个股传闻
- 与 Step 4 的板块上涨原因**不重复叙述**：要闻板块讲"发生了什么"，板块原因讲"为什么这个板块涨"

#### 排版清单（照着做，别省）

1. **深色主题**：底 `#0d1117`、面板 `#161b22`/`#1c2128`、正文 `#E6EDF3`、次要文字 `#8b949e`、分隔线 `#30363d`
2. **A股配色**：涨幅红色 `#ef4444`，下跌绿色 `#3fb950`，不混用欧美惯例
3. **顶部关键指标卡片区**：上涨家数、涨幅极差、双榜重合数等 3–5 个数字卡片，先给全貌
4. **双榜 Top 30 用可滚动容器**：`max-height` + `overflow:auto` + `thead` 设 `position:sticky`，避免页面被一张长表拖垮
5. **数字列右对齐 + `font-variant-numeric: tabular-nums`**，百分比一律带正负号
6. **上涨原因用编号徽标的分条卡片**：标题行是「板块名 + 红色涨幅 + 灰色标签（催化类型）」，正文一句核心逻辑 + 关键数字，不要糊成大段散文
7. **图表用纯 CSS 横向柱状图内联**，柱尾标数值，图例注明数据源
8. **正文容器最大宽度约 1180px，段落行高 ≥ 1.6**；分节用 `h2` + 左侧红色竖条，层级清楚
9. 数据说明/滞后提示放在开头（一行 meta 条 + 一条左侧蓝边的 note），风险提示用紧凑列表收尾
10. **要闻条目也用分条卡片**：左侧类型标签（六类配色区分），右侧一条消息；每条一眼扫完，不超两行

#### 内容结构

**近两日要闻 Top 10** → 数据说明 → 周涨幅 Top 30 表格 + 要点解读 + Top10 上涨原因 → 月涨幅 Top 30 表格 + 要点解读 + Top10 上涨原因 → 三条主线 + 风险提示 + 免责声明。

**Top 30 必须给全，不要缩成 Top 10**；要点解读、主线、风险用紧凑列表，控制在能快速扫读的长度。

---

## 关键注意事项

1. **只看涨幅**：不生成跌幅榜，不做"最弱方向""资金撤离方向"分析；图表柱子统一红色 `#ef4444`，不使用绿色。
2. **输出 HTML 报告**：报告是一份自包含单文件 HTML（样式内联、无外部依赖），排版按 Step 5 的清单执行；生成后 `present_files` 呈现，对话里只给 10 行以内的摘要。用户明确要求，2026-09-18。
3. **开篇必须有近两日要闻 Top 10**：范围是报告生成当天 + 前一天，最多 10 条，每条不超过 100 字，只收能传导到股市的消息。用户明确要求，2026-09-18。
4. **数据滞后 1~2 个交易日**。`index_hist_sw` 返回的 `latestDate` 通常比当前交易日早 1 天，偶尔 2 天；以抓取实际返回的 `latestDate` 为准，必须在报告开头显式声明，并在风险提示里说明最新一两个交易日未纳入。
5. **僵尸指数必须剔除**，否则它们的"最新价"是几个月前的，会严重污染排名。
6. **涨跌幅为 `None`** 表示历史数据不足 N 个交易日，排序时沉底（即使全榜上涨，也不要把 `None` 当成 0%）。
7. **同源归因合并**：申万二级内部高度相关（如光伏设备/电池/风电/电网设备同属新能源），讲原因时要分层到一级行业逻辑，避免碎片化。
8. **广度统计要算全样本**：`calc_changes.py` 输出里的 `Up(5d)` 只覆盖被 `--limit` 截断后的行，不能当作全市场上涨家数。
9. **分析免责**：基于公开市场数据与新闻整理，仅供研究参考，不构成投资建议。
